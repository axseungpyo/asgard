# RP-001: Olympus 공개 배포 구조 구축

## Summary

README.md, LICENSE, .gitignore, VERSION을 신규 생성하고 install.sh를 원격/로컬 이중 실행 지원으로
수정했다. curl | bash 원격 설치와 git clone 후 로컬 설치 모두 지원한다.

## Files Changed

| Path | Action | Rationale |
|------|--------|-----------|
| README.md | created | 설치 방법, 에이전트 소개, Skills 목록, CLI 명령어 문서 |
| LICENSE | created | MIT 라이선스 전문 |
| .gitignore | created | 프로젝트 런타임 파일 제외, 프레임워크 파일 포함 |
| VERSION | created | 초기 버전 0.1.0 |
| install.sh | modified | 원격/로컬 자동 감지, GitHub 다운로드 로직 추가, olympus update GitHub 연동 |

## Commands Executed

| Command | Result | Notes |
|---------|--------|-------|
| bash -n install.sh | PASS | 문법 오류 없음 |
| bash install.sh | PASS (로컬) | Skills 재설치 정상 |

## Acceptance Criteria Check

- [x] `curl -fsSL .../install.sh | bash` 형식 지원 — PASS
  (BASH_SOURCE[0] 비어있을 때 REMOTE=true 감지, GitHub에서 파일 다운로드)
- [x] `bash install.sh` 로컬 실행도 동일하게 작동 — PASS
  (BASH_SOURCE[0] 경로 있을 때 REMOTE=false, 기존 로직 유지)
- [x] README.md에 설치 방법, 사용 방법, Skills 목록, 라이선스 포함 — PASS
- [x] README.md에 olympus new / doctor / update 명령 설명 포함 — PASS
- [x] LICENSE 파일에 MIT 라이선스 전문 포함 — PASS
- [x] .gitignore에 artifacts/handoff/, artifacts/logs/, shared/context.md, src/ 포함 — PASS
- [x] VERSION 파일 내용: 0.1.0 — PASS

## Known Issues / Follow-ups

- `GITHUB_USER="YOUR_GITHUB_USERNAME"` placeholder가 install.sh와 olympus CLI에 남아있음
  → 실제 GitHub 저장소 생성 후 두 곳을 실제 값으로 교체 필요
- `olympus update`는 GitHub 저장소가 실제로 존재할 때만 동작함
- README의 설치 URL도 실제 username으로 교체 필요

## Recommended Next Actions

1. GitHub 저장소 생성: `gh repo create olympus --public`
2. install.sh의 `GITHUB_USER` + olympus CLI의 `OLYMPUS_GITHUB_USER` 실제 값으로 교체
3. README.md의 `YOUR_GITHUB_USERNAME` 실제 값으로 교체
4. `git init && git add . && git push`
5. (선택) TP-002: GitHub Actions CI 추가 (install.sh 자동 검증)

## Context Digest (for Odin)

- Architecture: 단일 install.sh가 로컬/원격 모두 처리. REMOTE 플래그로 분기.
- Key entry points: `install.sh` (설치), `~/.olympus/bin/olympus` (CLI)
- GitHub placeholders: `YOUR_GITHUB_USERNAME` 3곳 교체 필요
  - install.sh:4 (GITHUB_USER)
  - ~/.olympus/bin/olympus:3 (OLYMPUS_GITHUB_USER) — install.sh 재실행으로 업데이트됨
  - README.md:57,62,99,103 (URL들)
- Gotchas: 원격 설치 시 GITHUB_USER가 placeholder이면 에러로 중단됨 (의도된 동작)
