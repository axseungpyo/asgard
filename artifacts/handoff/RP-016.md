# RP-016: 타입 경계 수정 — shared/types.ts 분리

## Meta
- **TP**: TP-016
- **Agent**: Brokkr (Codex CLI)
- **Mode**: Anvil (auto-edit)
- **Status**: review-needed
- **Completed**: 2026-03-11

## Summary
서버 코드의 `dashboard/lib/types` 역방향 의존을 제거하고, `shared/types.ts`를 도메인 타입의 유일한 원천(Single Source of Truth)으로 분리 완료.

## Changes Made

### New Files
- `src/yggdrasil/shared/types.ts` — 도메인 타입 정의 (AgentName, AgentStatus, AgentState, TaskStatus, Task, LogEntry, OdinMessage, OdinAction, DependencyGraphNode, DependencyGraphResponse)
- `src/yggdrasil/shared/constants.ts` — 에이전트 설정 (AGENT_CONFIG, AGENT_NAMES, AgentConfig)

### Modified Files (import 경로 변경)
- `server/agents.ts` — `../dashboard/lib/types` → `../shared/types`, `../dashboard/lib/constants` → `../shared/constants`
- `server/routes.ts` — 동일
- `server/control.ts` — `../dashboard/lib/types` → `../shared/types`
- `server/task-manager.ts` — 동일
- `server/parser.ts` — 동일
- `server/dependency.ts` — 동일
- `server/watcher.ts` — 동일
- `server/odin-channel.ts` — 자체 OdinMessage/OdinAction 정의 제거, `../shared/types`에서 import

### Re-export Files
- `dashboard/lib/types.ts` — `export * from "../../shared/types"` + UI 전용 타입(StatusResponse, ChronicleResponse 등) 유지
- `dashboard/lib/constants.ts` — `export * from "../../shared/constants"` + UI 전용 상수(STATUS_CONFIG, TASK_STATUS_CONFIG 등) 유지

### Test Updates
- `server/__tests__/agents.test.ts` — import 경로 변경
- `server/__tests__/constants.test.ts` — shared/constants import로 변경, UI 전용 테스트 제거

## Acceptance Criteria Verification

| # | Criteria | Result |
|---|---------|--------|
| 1 | `src/yggdrasil/shared/types.ts` 존재 + 모든 도메인 타입 정의 | PASS |
| 2 | server/ 내 `dashboard/lib/types` import 0개 | PASS (grep 0 matches) |
| 3 | server/ 내 `dashboard/lib/constants` import 0개 | PASS (grep 0 matches) |
| 4 | `dashboard/lib/types.ts` re-export로 기존 컴포넌트 호환 | PASS |
| 5 | `tsc --noEmit` 에러 0개 | PASS |
| 6 | 서버 재시작 후 /api/health 정상 응답 | SKIP (샌드박스 제한, Odin 검증 필요) |
| 7 | vitest run 전체 PASS | PASS (6 files, 31 tests) |

## Notes
- 서버 health check는 Codex 샌드박스의 포트 바인드 제한으로 미검증. Odin이 수동 확인 필요.
- `dashboard/lib/constants.ts`의 `AgentConfig` 타입이 `shared/constants.ts`로 이동됨.
